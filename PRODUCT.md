# CM Agent 产品上下文

<!-- impeccable:product-schema 1 -->

## Platform

web

## 用户

- 主要用户为平台管理员与开发者，优先满足配置、接入、调试和治理需求。此优先级已于 2026-09-20 由用户确认。
- 管理员需要理解资源状态、授权范围及操作后果；开发者需要接入模型和工具、验证 Agent 行为，并定位运行失败。
- 是否另设面向业务人员的独立使用入口，尚未确定；不能据此改造当前管理控制台。

## 产品目的

CM Agent 是基于 AgentScope Java 的企业级智能体开源底座，提供 Java SDK/Core、Spring Boot Starter、独立服务端及轻量控制台。控制台让使用者完成从模型配置、Agent 与工具接入，到会话验证、运行排查和审计追踪的工作。

成功意味着使用者能识别缺少的前置条件、完成首次有效调用，并理解权限、资源状态或执行失败的原因；不以创建了多少配置作为唯一成功标准。

## 产品定位

以 Java 服务端的受控执行和治理能力为基础，将模型配置、工具授权、运行记录、多租户隔离与安全审计连接起来。控制台是这些真实能力的操作入口，不是仅展示指标的看板，也不是让浏览器任意执行工具代码的开发环境。

## 使用场景与流程

- 首次配置：准备当前租户可用且已启用的模型配置，再创建 Agent，按需要接入和授权工具，随后验证会话或运行。
- 工具治理：明确工具定义及运行条件，再处理授权和调试；MCP 发布是具有独立校验的治理操作，不代表已获得全部调用权限。
- 日常使用：会话聊天承载持续交互；运行记录承载执行排查、历史和工具调用详情；审计日志追踪主体操作及结果。三者职责不能仅因内容相关就合并。
- 现有控制台为服务端提供的 HTML、CSS、JavaScript 网页。默认入口为 `/console/v2/login.html`；v1 保留为兼容入口。不是原生移动应用。
- 本地调试与测试必须显式选择对应运行 profile；示例能力或开发配置不能被宣传为生产默认能力。

## 能力与约束

- 保留能力总览、模型配置、Agent 管理、工具治理、会话聊天、运行记录和审计日志这些已有业务职责及直接访问页面的能力。
- 租户归属与权限来自服务端认证上下文；前端隐藏按钮不能替代后端授权、租户过滤和审计。
- 模型凭据与工具 Secret 不得回显；本地验证账号和密码不得进入文档、页面、日志或版本控制。
- HTTP 工具执行受服务端网络、安全和资源限制；LOCAL 工具执行器须在服务端注册，不能通过页面上传或编写任意执行代码。
- 内置示例目录受环境、租户和权限约束，不等于所有租户都能使用的工具市场。
- 工具定义、运行时就绪、启用、授权和 MCP 发布是不同状态，不能用一个“可用”标签替代全部判断。
- 对有引用关系或历史记录的资源，遵守服务端删除限制；保留历史与审计链路，必要时引导停用或解除关联。
- 失败提示应给出可行动的中文原因；需要排查的失败显示稳定错误码和错误编号，不泄露内部堆栈及敏感信息。
- 响应式适配不得改变权限与操作语义。具体无障碍等级、支持浏览器清单和特殊辅助需求尚未约定，不声明已达标。

## 品牌与语言承诺

- 产品名称保持为 CM Agent；界面及面向使用者的说明以中文为主。
- Agent、Tool、MCP 等技术名称与协议字段按既有约定使用；解释以实际操作和结果为中心，不夸大功能或安全承诺。
- 当前控制台是后续迭代的业务与交互基线；替换整体界面需要独立确认。本文件不规定颜色、字体、间距或组件样式。

## 已有证据

- 产品与使用说明：[README.md](README.md)。
- 运行约束与配置：[docs/configuration.md](docs/configuration.md)、[docs/operations.md](docs/operations.md)。
- 真实控制台页面：`cm-agent-console/src/main/resources/META-INF/resources/console/v2/`；共享行为：`cm-agent-console/src/main/resources/META-INF/resources/assets/`。
- 当前迭代记录：`docs/superpowers/progress/2026-09-20-first-agent-onboarding-ledger.md` 与 `docs/superpowers/progress/2026-09-20-tool-workspace-stages-ledger.md`。记录中的验证有时间与范围限制，不等于所有环境都已验收。
- 没有为本次初始化提供可核验的客户背书、商业定价、性能基准或合规认证；后续设计不得自行补造。

## 产品原则

1. 让前置条件和下一步清楚可见，帮助管理员与开发者完成真实任务。
2. 一次聚焦一个操作，保留当前资源上下文，避免把全部管理动作同时堆给使用者。
3. 清楚区分资源状态与权限状态，界面表达必须与服务端行为一致。
4. 错误可理解、可定位、可恢复，同时守住凭据、租户和审计边界。
5. 用现有业务与验证证据约束迭代，不虚构能力，不以视觉优化扩大产品范围。
