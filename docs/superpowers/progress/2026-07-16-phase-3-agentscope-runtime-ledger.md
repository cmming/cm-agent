# 阶段 3 AgentScope Runtime 进度记录

| 计划项 | 状态 | 证据 | 发现的问题 | 修复结果 |
|---|---|---|---|---|
| Task 1：Core 运行契约 | 已完成 | JDK 21.0.11、Maven 3.9.4；审查修复后 Core 指定 22 个、Core 全量 52 个、Adapter 全量 2 个、Server 指定 30 个测试通过；Server `clean compile` 与全部测试源码编译通过；`git diff --check` 通过 | 独立审查发现 Adapter/Server 旧构造调用断裂、Agent 与模型 ID 未绑定、工具调用空白标识及部分上下文可绕过校验、Fake Runtime 未复用 runId；最终回归发现同时间戳审计事件的首项断言不稳定 | 已前移最小 `ModelConfigRepository` memory 契约和运行加载链路，修复全部上下文不变量与调用点；审计测试改为验证目标事件存在；JDBC Repository 仍留给 Task 2 |
| Task 2：模型配置仓储 | 已完成 | RED `21a231dff28179b03ceef6d604a364f4a04e46fb`：本地 Core 10 个测试中新增 2 个失败，Persistence 测试编译因缺少 `JdbcModelConfigRepository` 失败；Rocky 远程同 SHA 复现缺类。GREEN `2240ee8904c3e36761bb04aa33e5a6c48c7625c8`：远程 `maven:3.9.9-eclipse-temurin-21`（JDK 21.0.7）中 PostgreSQL 16 与 MySQL 8.4 指定测试 4 个、JDBC Web 测试 7 个全部通过；本地 memory/JDBC 装配与 Core 分类校验通过，Server `clean test-compile` 通过 | Task 1 前置的 Core/memory 子集导致 JDBC profile 缺少 `ModelConfigRepository` Bean；`AgentRunRequest` 对 Agent 模型 ID 和模型配置 ID 的空值缺少可分类中文消息；跨模块指定测试需设置 `surefire.failIfNoSpecifiedTests=false` | 新增固定租户条件的 `JdbcModelConfigRepository`，完成 JDBC Bean 装配与 PostgreSQL/MySQL 租户隔离覆盖，JDBC profile 上下文可启动；显式拒绝两个空 ID 并给出不同中文消息 |
| Task 3：外部模型凭据 | 已完成 | RED：JDK 21.0.11、Maven 3.9.4 下目标测试编译因缺少 `AgentScopeRuntimeProperties`、`ExternalModelCredentialProvider` 失败；GREEN：指定测试 13 个通过；配置与 profile 合理回归共 60 个测试通过；`git diff --check` 通过 | 首次 RED 命令中的未加引号 `-D` 参数被 PowerShell 错误解析，未进入预期编译阶段；安全自检要求覆盖同模型跨租户隔离、重复复合键、空值与范围、凭据列表复制、异常和字符串脱敏、fake/real 互斥 | 为 PowerShell Maven 属性参数加引号后复现正确 RED；实现复合键不可变凭据快照、完整属性校验及脱敏字符串表示，全部目标与回归测试通过 |
| Task 4：工具治理网关 | 已完成 | 初始 RED 因缺少 `GovernedToolInvocationService` 编译失败，初始 GREEN 目标 9 个、非容器 Server 回归 128 个测试通过；独立审查修复 RED 为目标 14 个测试中 3 个按预期失败，最终 GREEN 为目标 16 个、同范围 Server 回归 135 个测试通过；环境 JDK 21.0.11、Maven 3.9.4，`git diff --check` 通过 | 首次 GREEN 的完成审计失败用例因 Mockito 严格桩未正确表达同一 `append` 的连续调用；独立审查发现 registry 定义缺失或 tenant、ID、名称不一致时未写固定失败审计，且缺少同一 Service 调用期间撤销授权、禁用定义、请求名称不匹配的显式回归保护；本机不能运行 3 个 Testcontainers/JDBC Web 测试 | 使用连续调用桩修复测试建模；registry 不可用时在 started/execute 前写固定 `TOOL_CALL_FAILED`，审计异常原样传播；新增逐次授权撤销和安全短路测试，并参数化覆盖 registry tenant、ID、名称漂移，目标与非容器回归全部通过；本任务不涉及 JDBC、Flyway 或容器变更 |
| Task 5：AgentScope 模型与工具桥接 | 已完成 | 初始 RED：目标测试在 `testCompile` 因缺少两个 Provider 包及三个目标类按预期失败；独立复审 RED：Core 基础设施异常契约缺失导致编译失败，模型原始工具名测试明确得到 `expected model-supplied-name but was echo`；最终审查 RED：fatal 通道测试因 `throwIfInfrastructureFailure()` 缺失编译失败；最终 GREEN：Adapter 目标 17 个、Adapter 全量 19 个、Core 全量 56 个、此前 Server 非容器审计回归 23 个测试全部通过；`dependency:tree` 中三个 AgentScope 工件均为 `2.0.0`，无 RC | 首次 RED 的未加引号 `-D` 参数被 PowerShell 错误解析；AssertJ 3.27.3 无 `isNotNegative()`；AgentScope 2.0.0 的结果静态工厂默认状态为 `RUNNING`；首轮审查发现合法 Schema null 问题；独立复审发现严格审计失败被吞、模型实际工具名被覆盖；最终复核发现 AgentScope `ToolExecutor` 会用 `onErrorResume` 再次吸收已重抛的基础设施异常 | 修复 Maven 参数、结果完成态和 null Schema；Core 基础设施失败契约由 Server 审计异常继承，Bridge 原样抛出；模型原始工具名交给治理网关；Bridge 用 `AtomicReference` 首次保存 fatal，并提供 `throwIfInfrastructureFailure()` 只读动作通道，被 reactive 链吸收后仍抛同一实例，普通异常不设置，后续并发 fatal 不覆盖；Provider 保持 optional，记录保持线程安全与不可变快照 |
| Task 6：真实 ReAct 执行器 | 已完成 | 初始 RED：目标测试 `testCompile` 因缺少 `AgentScopeExecutor`、`AgentScopeExecutionResult`、新 RunSpec API、Adapter 构造器和公开工厂退出 1；安全优先级 RED：DENIED 后第二轮 Provider 失败错误地抛出 Provider 异常；HTTP 脱敏 RED：真实 500 响应直接抛 `OpenAIException`；审查修复 RED：先缺少生命周期观测接口，随后证明 50ms 在首轮冷启动即超时而未进入组合路径。最终 GREEN：目标 18 个、Adapter 全量 35 个、Core 56 个测试全部通过，失败 0、错误 0、跳过 0；无工具合同请求 1 次并断言最终文本“真实运行成功”，工具合同请求 2 次并断言最终文本“工具运行后的最终回答”；依赖树三个 AgentScope 工件均为 `2.0.0` | AgentScope 2.0.0 模型工厂固定流式请求，JDK transport 要求 SSE `data:` 帧，不能直接返回裸 JSON；`ModelUtils` 的 timeout `ModelException` 没有 `TimeoutException` cause；OpenAI HTTP 500 抛出的 `OpenAIException` 实现 `ModelHttpException`，不继承 `ModelException`；ToolExecutor 会把 gateway fatal 转为普通工具错误块；模型超时取消 JDK HTTP future 会产生 Reactor dropped cancellation；只读审查发现已有 DENIED 后下一轮 timeout 会先返回而跳过 interrupt，且生命周期与每 run 新实例缺少观测证据 | 本机 HttpServer 将计划 JSON payload 原样封装进 SSE 帧且不访问外网；兼容 2.0.0 timeout 固定前缀；Provider 分类辅助方法识别 `ModelHttpException` 能力接口但不把它放进 catch 类型列表，统一返回固定中文；正常流和异常流都先遍历全部 Bridge 的 `throwIfInfrastructureFailure()`，真实 Toolkit 合同证明 gateway fatal 被转块后仍传播同一实例，且后续 Provider 失败也不能覆盖；fatal 后先识别 timeout 并执行 interrupt，再让任何拒绝记录强制 Run 为 `DENIED`；包内生命周期观测合同证明成功 close、DENIED 后 timeout 同时 interrupt/close，以及连续两次运行的 Agent、Model、Toolkit、RuntimeContext 均为不同实例且上下文完整；超时合同用局部 Reactor hook 接管预期取消噪音 |
| Task 6：第三轮并发与生命周期复审修复 | 已完成 | RED：两个既有并发合同共 32 个测试中 2 个失败，分别观测到被中断等待者仍进入网关、超时后迟到结果仍写工具记录；新增 3 个审查合同均先失败，证明 fatal 的 interrupt 异常丢失、受控 Provider 失败吞掉 close 异常、普通 Reactor `CANCEL` 被误标工具超时。GREEN：两个核心合同 35 个、Adapter 全量 47 个、Core 全量 56 个测试通过，失败 0、错误 0、跳过 0；两个并发边界定向连续 3 轮通过；`git diff --check` 通过；Authorization 精确断言 `Bearer <测试占位凭据>` | Java monitor 等待不可中断；网关返回后没有再次检查 fatal/timeout；catch 中直接返回受控结果时，`finally` 把 close 异常附加到不会再抛出的异常；AgentScope 2.0.0 的工具 API 只暴露通用 `CANCEL`，不暴露取消原因 | 每 run 使用公平 `ReentrantLock` 和 `lockInterruptibly()`；网关前后都按 fatal→timeout 检查，等待取消和迟到结果均不能产生外部调用或记录；interrupt/close 失败作为 suppressed 保留在首个 fatal；受控返回前清除内部 primary，使 close 失败传播；仅当取消时已达到配置工具时限才标记工具超时，并以中文注释记录 2.0.0 限制 |
| Task 6：第四轮取消信号与异常链复审修复 | 已完成 | RED：非零 20ms 工具时限下运行 50ms 后普通主动取消错误地设置 timeout；工具 timeout 同时发生 lifecycle interrupt/close 失败时实际只抛 close；完整合同还复现首个并行调用被超时中断后、明确超时结果事件到达前第二个等待者进入网关。GREEN：两个核心合同 37 个、Adapter 全量 49 个、Core 全量 56 个测试通过；四个并发/迟到边界连续 3 轮通过；`git diff --check` 通过 | AgentScope 2.0.0 的 ToolExecutor 在 Bridge 外层实施 `Mono.timeout`，上游 Bridge 只能收到无原因 `CANCEL`；按 elapsed 猜测会把普通或 fatal 取消误标为 timeout；首次 interrupt 失败发生在事件回调中时，后续重复中止被一次性门跳过，原失败未进入最终异常链；网关中断释放锁早于超时结果事件 | Bridge 不再根据 `CANCEL` 推断；Executor 仅识别 AgentScope ToolExecutor 针对当前配置产生的 `ToolResultTextDeltaEvent` 明确超时文本；中断调用使用独立状态先阻断同 run 后续网关副作用但不设置 timeout；RunGate 保存首次 lifecycle interrupt 失败，Executor 将其作为主异常并把 close 失败加入 suppressed；普通长时取消与 fatal 均不污染 timeout |
| Task 7：Server 装配与运行链路 | 未开始 | 尚无 | 尚无 | 尚无 |
| Task 8：文档与整体验证 | 未开始 | 尚无 | 尚无 | 尚无 |
