# 动态 HTTP 工具与 MCP 发布进度账本

| 任务 | 状态 | RED | GREEN/回归 | 审查修复 | commit |
| --- | --- | --- | --- | --- | --- |
| Task 1：核心领域与依赖契约 | 已完成 | 新类型和结果工厂缺失，`cm-agent-core` 测试编译失败 | 指定测试 11 项通过；完整 core 回归通过；server 跳过测试打包通过 | 双阶段审查通过；`mcp:0.17.0` 为 0 个 class 的元数据 JAR，未与 MCP 2.0.0 重复 | `70736c5` |
| Task 2：V4 与 JDBC/memory Repository | 已完成 | Rocky 容器在 `c5e030d` 上因两个 JDBC Repository 缺失而 testCompile 失败；`5221fdc` 覆盖 Secret 引用与并发首次保存 | Rocky 容器最终通过核心 3 项、PostgreSQL 4+3 项、MySQL 6 项、迁移 2 项及 server 配置 10 项；PG/MySQL 双实例并发保存均通过 | 首轮补齐 memory 删除；审查后增加 `secret/` 安全引用约束、父工具行事务锁和双数据库并发测试，复审 Critical/Important 均关闭 | `c5e030d`..`ba9a1c9` |
| Task 3：HTTP 工具创建链路 | 已完成 | 初始 4 项 API RED；两次审查 RED 覆盖无效配置 memory 残留、唯一索引冲突、空映射、批量查询契约缺失，以及 memory 同名并发与按名称回查响应错误 | 本地 Management 4、Controller 7、memory 配置 8、RunController 25 项通过；Rocky Linux 的 `maven:3.9.9-eclipse-temurin-21` 容器在 `4fdfce3` 上完成 PostgreSQL/MySQL 同名并发及审计失败回滚 4 项验证 | 补齐 memory 补偿、精确冲突映射、批量 tenant 查询、原子 tenant/name 索引和按 ID 响应；未提前实现 Task 4 深层校验 | `36d775f`、`37b1823`、`4fdfce3` |
| Task 4：输入校验与参数映射 | 已完成 | 八轮复审最终补齐线性 `$ref` 与深 `properties/items` 调用栈边界；既有 RED 覆盖 discriminator oneOf、Schema-bearing 子树引用、数组索引、引用链绕过、重复 DAG 与宽 Schema | Validator 30、Mapper 13、Management 5、ToolController 7，共 55 项通过；networknt 依赖树确认 2.0.0 | 调用局部上下文独立限制 256 层活动递归和 10,000 个遍历状态；缓存按 schema path 与直接引用链隔离；257 层固定拒绝、256 层正常，历史环检测与合法递归保持 | `29b01fa`、`e4587a9`、`537274f`、`37caa27`、`0dfde42`、`ac35d45`、`87ad973`、`bee1046`、本提交 |
| Task 5：安全 HTTP 执行器 | 已完成 | Secret/属性、URL 策略、执行器、profile 均先观察到缺类、缺行为或错误语义失败；复审 RED 继续覆盖跨源重定向、统一 deadline、IPv6 特殊前缀、歧义响应头、结构化脱敏、阻塞/累计 Secret Provider 及 Provider 捕获中断后错误继续循环 | Secret、URL、执行器、profile 最终指定 144 项通过；跳过测试的 reactor 打包通过 | 同源重定向、严格 profile、统一 deadline、可关闭传输、保守 IPv6、identity 编码、递归 JSON/text 脱敏和序列化后二次限长已补齐；完整 Secret 循环纳入同一 deadline，并在每次 Provider 调用前后及异常路径传播取消状态，超时后不查询剩余引用且 DNS/网络零调用。自定义 Provider 必须响应中断并配置自身 I/O timeout；忽略中断时调用方可及时返回，但后台虚拟线程可能等到 Provider 返回。JDK HttpClient 仍保留 DNS TOCTOU 风险并要求基础设施 egress | `e7e444c`、`041929d`、`64e3594`、本提交 |
| Task 6：统一治理执行入口 | 已完成 | 新统一服务缺失、网关新依赖不匹配时，Temurin 21 下定向测试按预期 testCompile 失败；阻断修复时 `prepare` 与执行令牌缺失再次按预期 testCompile 失败；LOCAL 快照与单次令牌测试在 clean 后观察到替换后执行新 executor、并发重复副作用 | 统一服务、网关、core registry、starter 配置测试通过；阻断修复后定向测试与跳过测试打包通过；`-am` 全量回归因上游 persistence Testcontainers 无 Docker 环境失败，本任务未执行容器验证 | HTTP 每次按 tenant/toolId 重读且 endpoint/urlTemplate 一致，LOCAL 校验注册定义 tenant/id/name；MCP/A2A、禁用、跨租户及不匹配均固定“工具不可用”。prepare 在 STARTED 前一次性解析，未就绪仅 FAILED；配置持久化异常原样上抛，不降级为工具失败；Agent 保持二次授权与严格完成审计。LOCAL 使用原子注册快照，ready 令牌 CAS 单次消费。`ToolRegistry.snapshot` 设为必须实现以编译期阻止自定义实现静默禁用 LOCAL，旧 `execute` 保留 | `0d0f135`、`dce200e`、本提交 |
| Task 7：调试与 MCP 发布服务 | 已完成 | 先观察到 ToolDebugService、McpPublicationService 与响应类型缺失导致指定测试 testCompile 失败；随后 URL/堆栈脱敏回归先失败；修复审查 RED 覆盖 STARTED 时机、快照漂移和 prepare 持久化异常；最后区分准备与执行阶段数据访问异常 | Temurin 21 下服务、治理、执行器、Controller/Auth 指定回归通过；跳过测试打包通过 | 调试经 `executeWhenReady` 在同一预备快照内写 STARTED；准备阶段 DataAccess 包装后由 DEBUG 展开为原异常且无审计；执行阶段 DataAccess 保持受控 FAILED；Dynamic HTTP 与 DEBUG 共用结构化输出脱敏；memory 发布补偿、JDBC 事务保持 | `8bbda09`、`4235557`、`d587983`、本修复提交 |
| Task 8：MCP Streamable HTTP 端点 | 已完成 | 官方客户端集成测试首跑发现 Spring ERROR 分派在认证上下文清理后将应有的 `404`/`405` 重写为 `401`；审查 RED 发现不可用路径未审计，且拒绝/运行时失败审计故障会逃逸 SDK handler | Temurin 21 下 MCP 目录/Servlet/配置/disabled/官方客户端集成共 32 项通过；覆盖 initialize、listTools、callTool、Schema 校验、取消发布、禁用、HTTP/LOCAL 漂移、JWT/权限、Origin/Host、GET 405、disabled 404、并发租户隔离与每请求关闭 | 放行 `DispatcherType.ERROR` 保留原始 HTTP 状态；不可用路径只写一次 `MCP_TOOL_CALL_FAILED`，其审计故障以及拒绝/运行时失败审计故障统一转换为不含敏感信息的 MCP 协议错误；正常请求仍要求 JWT；双阶段审查通过 | `0b7c20e`、`857b6ea` |
| Task 9：控制台与端到端验证 | 已完成（容器验证待 Rocky 环境） | Node v16 不支持 `node:test`，新增纯函数正式测试无法启动；资源契约测试先覆盖 HTTP、MCP、调试、安全渲染、路径占位符 URL 模板、发布锁和加载版本门闩 | Temurin 21 下 console 资源测试、ToolControllerTest 通过；Node 兼容断言与语法检查通过 | 前端仅接受 `secret/` 引用、动态内容使用 textContent、HIGH 名称双重精确确认；URL 模板使用文本输入；MCP 操作按工具锁定实际按钮，每次写入完成创建当前 reload 版本，最终状态服从最后完成的操作；ConsoleSmoke 未在本机运行 | 未提交 |
| Task 10：发布收尾与兼容性复核 | 已完成（审查修复后 Rocky 复验待控制器） | 文档契约首次因 README 缺少动态工具说明失败；最终独立审查 RED 又覆盖初始发布非法名称/审计回滚、前缀 API Key 脱敏、MCP LOCAL 超大输出、LOCAL 控制台发布入口及完整文档契约；审查后补充 RED 覆盖内存初始发布第二条审计失败残留成功事件，以及 `databaseSecret`/`vendorAccessToken`/`serviceRefreshToken` 结构化泄露；JDBC 批量审计的 PostgreSQL 与 MySQL 均覆盖第二条冲突回滚和外层事务加入 | Temurin 21 下动态工具、MCP 配置/Servlet/官方客户端集成、Controller、调试、脱敏、文档和 console 定向回归通过；审查后 `ManagementCommandServiceTest,AuditAppenderTest,ToolOutputSanitizerTest,McpPublishedToolCatalogTest,McpServerConfigurationTest` 通过；本机 JDK 21 下 `cm-agent-server -am -DskipTests package`、`JdbcPersistenceConfigurationTest` 与 `cm-agent-persistence -am -DskipTests test` 通过；Node16 三文件语法检查及 HTTP/LOCAL 发布入口兼容断言通过；修复前精确提交已在 Rocky Maven 3.9.9/JDK21 全量通过且 Node22 15 项通过，修复后容器测试仍需复验 | 初始发布与显式发布共享 MCP 名称/HTTP 配置规则并追加发布审计；初始 MCP 发布的两条成功审计改为批次，内存仓储原子可见；JDBC `appendAll` 使用同一事务模板，独立批次与初始 MCP 发布均可原子回滚；结构化输出识别常见敏感后缀且保留普通字段；MCP LOCAL 脱敏后执行 UTF-8 上限；HTTP/LOCAL 页面均可管理发布；独立文档契约锁定 enable、端点、GET405、egress | 未提交 |
