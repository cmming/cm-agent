# 首个 Agent 引导实施计划

1. 修改 `overview.html`，加入可更新的首次成功提示、三步进度与首要操作锚点。
2. 修改 `app.js`，让总览同时加载模型配置，并根据模型配置启用状态和 Agent 数更新引导。
3. 修改共享样式，提供紧凑、可访问的步骤状态视觉表达。
4. 更新全部 v2 页面资源版本，避免浏览器继续使用旧脚本或样式。
5. 更新资源测试并运行 JavaScript、Maven 与差异检查。
