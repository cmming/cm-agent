# Local test profile only. This file defines variables only.

cm-agent:
  config:
    jwt-secret: ${CM_AGENT_TEST_JWT_SECRET:}
    bootstrap-admin-enabled: true
    bootstrap-admin-username: admin
    bootstrap-admin-password: ${CM_AGENT_TEST_BOOTSTRAP_ADMIN_PASSWORD:}
    bootstrap-admin-display-name: 测试管理员
    fake-runtime-enabled: true
