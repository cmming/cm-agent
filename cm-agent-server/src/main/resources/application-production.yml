# Production profile only. This file defines non-secret defaults.
# Secrets and JDBC connection details must only be generated or mounted through controlled external YAML or a secret manager; do not inject them through environment variables.

cm-agent:
  config:
    allow-dev-jwt-fallback: false
    bootstrap-admin-enabled: false
    public-api-docs-enabled: false
    persistence-mode: jdbc
